[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=dev"

# SPA fallback — ensures any direct/deep link still serves index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200